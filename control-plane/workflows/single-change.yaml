# Moved to .agents/workflows/single-change.yaml under the CDCP
# operating-model install (spec 0011). This file remains as a pointer
# so existing references resolve. New work points at the new path.
moved_to: .agents/workflows/single-change.yaml
