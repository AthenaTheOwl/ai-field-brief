# research: publishing

Resend's contacts API creates a global contact and can attach it to a
segment. Resend's broadcast API creates and sends a message to a
segment and supports the unsubscribe token in the email body.

Vercel cron invokes an application route on a schedule. The route keeps
send behavior behind `CRON_SECRET` and exposes dry-run inspection for
verification.
